#include <bits/stdc++.h>
using namespace std;

#define all(X) begin(X), end(X)
#define ll long long int

typedef vector<hash<ll>> Sections;
typedef vector<hash<ll>> Tricks;

int main() {
    int N, K;
    cin>>N>>K;

    Sections s(N);
    Tricks t(K);

    // ler quantidade de sessões e manobra (N e K);
    // cria estrura para guardar os dados
    // guarda os dados
    
    // PARA CADA SESSAO: 
        // chama dp e retorna a soma da de bonificação da melhor sequencia
        //calcula e imprime pontuação final (retorno de dp * bonificação da sessao*qtd de manobra usadas)

    return 0;
}